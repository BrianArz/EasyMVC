@model List<Brand>

<h1>Brand</h1>

<table class="table">
	<thead>
		<tr>
			<th>Name</th>
		</tr>
	</thead>
	<tbody>
		@foreach(var brand in Model)
		{
			<tr>
				<td>@Html.DisplayFor(x => brand.Name)</td>
			</tr>
		}
	</tbody>
</table>
